# Предзапусковый разбор

Совет из шести проверяющих — RLS, экономика, воркер, клиент, злоупотребления,
корректность, — каждого перепроверял скептик с установкой «опровергни, пока не
докажешь». Поднято 52 находки, выжило 45, отбито 7.

**Закрыто 43. Осталось 2, обе низкие и обе — следствие того, что учёток нет.**

## Что было самым опасным

| Дыра | Как это выглядело | Где закрыто |
|---|---|---|
| Купленный аватар ломал отправку цен | Магазин продавал 28 аватаров, `CHECK` знал 17. Купил «Баристу» за 75 монет — и каждая твоя цена молча отбивалась, навсегда | 22 |
| Кран монет `menu_photos` | Цикл `curl` — монета за запрос, без ограничений | 22 |
| Кран монет `price_updates` | Геофенс пропускал всё, чему не нашёл координат | 22 |
| Три запроса гасили любую точку | Один человек с тремя вкладками мог посерить всю карту | 22 |
| `leaderboard` отдавал `device` | Публичное представление показывало ключ, по которому читается чужой баланс | 22 |
| Монета за неподтверждённую цену | Везде написано «подтверждённых», платили за сам факт отправки | 25 |
| Баланс вычитал покупки дважды | После первой покупки витрина показывала ноль и отказывалась продавать | app.js |
| Отказ сервера прятался за «Принято» | Цена исчезала молча; повтор к тому же терял `device` | app.js |
| Очередь для метро не досылала ничего | `markSent` помечал всё сразу — очередь всегда пустая | app.js |
| `__proto__` в `item_id` | Одна строка в базе гасила все фото и всю проверку цен для всех | app.js |
| Тап по фото складывал приложение | У `.lightbox` не было правила в CSS вообще | styles.css |

## Что теперь платит монеты

Начисляет только сервер, и только за подтверждённое:

| Повод | Монет | Потолок в сутки |
|---|---|---|
| Подтверждённая позиция | 1 | 20 |
| Первая цена в заведении | 2 | — |
| Фото меню | 1 | 5 |
| Правка цены на месте | 1 | 5 |
| Жалоба, с которой согласился владелец | 1 | 10 |
| Веха (10, 25, 50 …) | 10 | — |

«Серия» и «новая станция» из интерфейса убраны: их никто не начислял.

## Закрыто последним (26)

**Погашенные монеты не оставляют долга, и аватар остаётся у человека.**
Скрыл владелец позицию — монеты за неё сгорают, но купленное на них никто не
отбирает: `settle_debt` кладёт компенсирующую запись `writeoff`, и баланс
возвращается в ноль вместо минуса. Потратить сгоревшие монеты второй раз нельзя,
а отнимать уже надетый аватар — наказание не по размеру ошибки.

**Монеты и аватары — только через Telegram.** Проверка стоит внутри
`award_coins`, то есть накрывает сразу все поводы: позиции, фото, правки,
жалобы, вехи. `buy_avatar` отказывает тем же условием. Устройство считается
своим, только если воркер привязал его к телеграм-аккаунту после проверки
подписи (`tg_devices`). В браузере наград нет вовсе — и накручивать нечего:
чистка localStorage больше ничего не даёт.

Сайт при этом остаётся полноценной картой: цены видны, поиск работает, свою
точку сдать можно, и она попадает на карту наравне с остальными. Витрина в
браузере честно говорит, где живут награды, вместо пустых замков и нуля.

Проверено: `award_coins` для непривязанного устройства возвращает `null` и не
пишет ни строки; `buy_avatar` отвечает «Аватары живут в приложении Telegram»;
после гашения монет аватар остаётся, баланс — ноль.

## Итог

45 находок, 45 закрыто. Открытых компромиссов не осталось.

# Второй разбор — по коду самих правок

Первый совет проверял приложение. Правки по его находкам — 903 строки — не
смотрел никто, поэтому их разобрал второй совет: 33 находки, выжило 27.

## Закрыто

| Дыра | Что было |
|---|---|
| `device` работал как пароль | `views.device` и `submissions.device` аноним читал (колоночный revoke в Supabase инертен), а `coin_stats` и `buy_avatar` принимали device как пропуск. Проверено эксплойтом: чужой прочитал баланс и купил «Золотого» за чужие монеты. Теперь право действовать даёт секрет `nishemap.dkey`, который заводит только воркер после проверки подписи |
| Привязка ключа «первому спросившему» | Первая версия ключей открывала ту же дверь: чужой называл device раньше владельца и становился хозяином. Теперь у tg-устройства ключ заводит только воркер |
| Слабый генератор ключа | `Math.random()` → `crypto.getRandomValues`; на сервере md5 → sha256 |
| `identity_of` открыт анониму | SECURITY DEFINER без revoke: любой device отображался в номер телеграм-аккаунта в обход запрета на саму таблицу |
| Фото подтверждало цену без файла | `item_verified` верил любой строке `item_photos`; ссылка на чужой сайт печатала монеты. Теперь адрес должен вести в наше хранилище |
| Премия за заведение переживала скрытие | Гашение искало `ref = 'ui-<id>'`, премия лежала под «название\|адрес» — не совпадало никогда |
| Народные точки исчезали при фильтре метро | `submissionToVenues` ставил пустой `district`, и `visibleVenues` их отбрасывал |
| Ссылки пятничной рассылки умерли | `digest.mjs` строил id из uuid, а приложение с 8fb7b05 ищет по хешу «заведение\|адрес». Формула перенесена, совпадение проверено |
| Очередь досылала отвергнутое | Отказ сервера оставлял запись неотправленной, и она уходила снова при каждом запуске |
| Праздник монет в браузере | «+1 монета» показывалась там, где сервер не начисляет ничего |
| Фото из формы шло мимо проверок | Кнопка «📷 фото» отбивает вчерашний снимок, форма — нет, а на сервере фото сразу оплачивалось |
